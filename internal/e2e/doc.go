// Package e2e drives a real hub over a real websocket with real scripts:
// nothing here is mocked, because everything interesting about agentfleet
// happens in the seams between a connection, a pipe and a process group.
package e2e
